# Pre-ship DEFECT review — `converge/20260914-setup-sh-runtime-dir`

**Anchor verified.** `git rev-parse HEAD` → `1bab60cb8bfbcd9cd978fcbf67e25abaf56cac70`;
`git log --oneline main..HEAD` → 22 commits. Reviewed in
`/tmp/claude-1000/-home-ai-dev-claude-mem-lite/4894b5a2-c46c-436b-bac1-0d73cc2bd805/scratchpad/rev-defect`
(`git archive HEAD`, `node_modules` symlinked). The repo at `/home/ai/dev/claude-mem-lite` was
read only (`rev-parse`, `log`, `diff`, `show`, `ls-files`).

Counts: **P1 2 · P2 3 · P3 6.** Every P1/P2 below is **reproduced**, with the command and its
output. The "reasoned only" items are marked as such.

---

## P1-1 — the new inert-flag notice fires on `optimize --project` and `optimize --scope`, which are documented, implemented and working

**Where.** `mem-cli.mjs:3622` `ARGV_PARSED_COMMANDS = new Set(['adopt', 'unadopt', 'doctor'])`
— `optimize` is missing. `cmdOptimize` never calls `parseArgs`: it reads its own raw argv at
`mem-cli.mjs:3505` (`const scopeIdx = args.indexOf('--scope')`) and `mem-cli.mjs:3518`
(`const projectIdx = args.indexOf('--project')`). Both `scope` and `project` are in
`FILTER_FLAGS` (`cli/common.mjs:110,113`), so `suppliedFlags` records them — from the
`suggestUnknownFlags(parseArgs(cmdArgs).flags)` call at `mem-cli.mjs:3678`, which parses every
command's args — and `readFlags` never can.

This is the identical failure the branch already shipped and fixed once for `unadopt --all`
(c54e0d1). `cli/common.mjs:112` even states the rule — *"Anything read off argv must stay
out"* — and `mem-cli.mjs:3636` states the per-command half. `optimize` is the third raw-argv
command and it was not enumerated.

**What breaks.** The notice tells the user their filter was dropped and that *"the results
above are UNFILTERED"*, in the same breath as stdout showing the filter applied. On the
mutating arm it does this over the exact backfill recipe the tool prints to the user itself.

**Reproduction (ran).**

```
$ node cli.mjs optimize --project demo
[mem] 🔍 LLM Optimization Preview:
  Project filter: demo                      <-- the filter WAS applied
  ...
Scope: --project <name|.|current> to limit; --verbose for cluster details.
[mem] --project was ignored — `optimize` does not filter on it, so the results above are UNFILTERED. Run "claude-mem-lite help" for the flags this command reads.
exit=0
```

```
$ node cli.mjs optimize --run --task re-enrich --scope wide --max 5 --project demo
[mem] Running LLM optimization (scope: wide) (project: demo)...
  Re-enrich: 0 processed, 0 skipped
[mem] --project and --scope were ignored — `optimize` does not filter on them, so the results above are UNFILTERED. ...
exit=0
```

`optimize --scope wide`, `optimize --scope aliases` and `optimize --task re-enrich --scope
wide` reproduce the same way. Found by a 3-batch sweep of 145 real invocations across every
CLI command and sub-action (script:
`.../scratchpad/sweep.sh` + `cases.txt`); these four were the **only** false alarms in the
sweep — every other correct usage was silent, including `unadopt --all`, `doctor --metrics`,
`memdir-audit --all`, `restore --project`, `import-jsonl --project`, `defer drop --project`
and the whole `activity`/`maintain`/`compress`/`export` families.

**Why the guard did not catch it.** `tests/cli-inert-filter-flag.test.mjs` enumerates by
command FAMILY, not by routing path: its controls are `browse / search / export / recent /
recall / stats / timeline`, its exemption cases are `unadopt` and `doctor`. The word
`optimize` does not appear in the file. `optimize` is the one command that is neither
routed-before-the-DB nor `parseArgs`-based — a DB command that parses raw argv.

---

## P1-2 — `scripts/post-tool-use.sh` writes `reads-<project>.txt` where Node does not read it, under `CLAUDE_MEM_RUNTIME_DIR`

**Where.** `scripts/post-tool-use.sh:80` `_data_dir="${CLAUDE_MEM_DIR:-$HOME/.claude-mem-lite}"`
→ `:110` `runtime_dir="${_data_dir}/runtime"` → `:120` appends to
`"${runtime_dir}/reads-${project}.txt"`. The Node side reads it at `hook.mjs:443`
(`join(RUNTIME_DIR, \`reads-…\`)`) and reaps it at `hook-shared.mjs:190`
(`f.startsWith('reads-') && f.endsWith('.txt')`), where `RUNTIME_DIR` is
`resolveRuntimeDir(resolveDataDir(CLAUDE_MEM_DIR))` — and `lib/resolve-data-dir.mjs:143-152`
makes that honour `CLAUDE_MEM_RUNTIME_DIR`. The bash side does not know that variable exists.

This is byte-for-byte the split commit ddbeeb2 just closed for `.deps-broken` in
`scripts/setup.sh`, whose own new comment says it survived because "`walkShipped` … is every
shipped .mjs/.js — a bash hook is structurally outside that population". The fix swept
`setup.sh` and stopped; the sibling bash hook with the same defect was not swept, and the new
guard written for exactly this class (`tests/reads-file-name-cross-language.test.mjs`) checks
the file's **name** (prefix `reads-`, suffix `.txt`) and never its **directory** — while its
own docblock quotes the harm as *"a runtime dir resolved differently on the two sides"*.

**What breaks.** Both halves the writer's comment at `:77-79` names: the Read fast path's file
context is silently dropped from every episode, and the bash-written file is outside the
reaper's directory so it is never collected and grows without bound.

**Reproduction (ran).**

```
$ echo '{"tool_name":"Read","tool_input":{"file_path":"/x/y/z.mjs"}}' \
  | env HOME=$S/home CLAUDE_MEM_DIR=$S/data CLAUDE_MEM_RUNTIME_DIR=$S/rt \
        CLAUDE_PROJECT_DIR=$S/proj bash scripts/post-tool-use.sh
bash exit=0

where bash wrote     : /tmp/tmp.el7JtuIFEy/data/runtime/reads-tmp.el7JtuIFEy--proj.txt
where Node reads/reaps: /tmp/tmp.el7JtuIFEy/rt/reads-tmp.el7JtuIFEy--proj.txt
```

(the second line printed by importing this tree's own `hook-shared.mjs` `RUNTIME_DIR` +
`utils.mjs` `inferProject()` under the same env.)

**Scope note.** Pre-existing, not introduced by these 22 commits. It is reported as P1 because
this branch is the one that went looking for this defect class, fixed one of its three shipped
bash-hook sites, and shipped a guard whose criteria cannot see the other.

---

## P2-1 — the "query echo" fix (d027169) missed a third echo site, and the residual echo is LARGER than the input

**Where.** `server.mjs:429`:
`if (expanded !== args.query) hint.push(\`Searched as: ${expanded}\`);` where
`expanded = ftsQuery || args.query`. Six lines above it, `:423` and `:452` were both wrapped
in `queryLabel()`. This branch is in the same function (`formatSearchOutput`), on the
zero-results path.

`tests/query-echo-bounded.test.mjs:81-83` explicitly dismisses this branch: *"A long query
that matches nothing lands on a third branch ('No results found.' + a spelling tip) which
never renders the label at all."* It does not render the *label* — it renders the **expanded**
query, which synonym expansion makes bigger than the input.

**What breaks.** Precisely the harm the commit message and the test docblock state: an MCP
search result carrying several KB of the caller's own input back into the model's context, on
a tool whose purpose is to spend that context carefully.

**Reproduction (ran)** — via this tree's own `handleSearchForTest` + `createTestDb`:

```
input query chars : 10329
MCP result chars  : 13691
ratio out/in      : 1.33
starts with       : "No results found.\nSearched as: (database OR db OR 数据库) AND connection AND (timeout OR deadline OR 超时) AND stack AND trace AND errorword0 AND erro…"
```

Unit-level confirmation: `buildSearchFtsQuery(q)` on that query returns 13,580 chars for a
10,329-char input.

---

## P2-2 — doctor's new never-deployed verdict says "none present" about an install where 40+ managed files are present

**Where.** `install.mjs:2379`
`const noCodeInstall = !shape.managed && !shape.activePluginVersion && !hasAnyManagedCode(INSTALL_DIR);`
and the message at `install.mjs:2452`. `hasAnyManagedCode` (`lib/install-shape.mjs:133`) tests
`MANAGED_ENTRY_POINTS.some(...)`, and that list is exactly
`['server.mjs', 'hook.mjs']` (`lib/install-shape.mjs:52`) — two files. The message it gates
makes a claim about the whole 43-file managed list.

**What breaks.** An install missing those two entry points but holding everything else is
reported as never-deployed. The verdict text is false ("none present"), it contradicts its own
parenthetical ("43 file(s) absent, none present" where far more than 43 files exist there), and
it withdraws the cheaper remedy — `repair` is runnable in that state, because `cli.mjs` is
present.

**Reproduction (ran).** Fixture: `$HOME/.claude-mem-lite/` containing `cli.mjs`, `install.mjs`,
`utils.mjs`, every `lib/*.mjs` and every `cli/*.mjs`; `server.mjs` and `hook.mjs` deliberately
absent.

```
$ env HOME=$S MEM_NO_AUTO_ADOPT=1 CLAUDE_MEM_DIR= node install.mjs doctor --json
- Managed files: no claude-mem-lite code is deployed in /tmp/tmp.lv5jlxw7OD/.claude-mem-lite
  (43 file(s) absent, none present) — this is a data directory with no install behind it,
  not a damaged one. Fix: node .../install.mjs install
```

**Why the guard did not catch it.** `tests/doctor-remedy-runnable.test.mjs:95-118` does have a
damaged arm, but its fixture writes `cli.mjs`, **`server.mjs`** and `mem-cli.mjs` — it keeps one
of the two discriminator files, so it exercises `hasAnyManagedCode → true` and cannot reach the
state above. Single-shape fixture, structurally blind to the neighbouring population.

---

## P2-3 — no CHANGELOG entry and no version bump for user-visible behaviour

`git log --oneline main..HEAD -- CHANGELOG.md` is **empty**; `package.json`,
`.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` all still read `6.8.3`,
which is the released version. Shipping as-is publishes, with no release note:

- a **new stderr line** on any CLI invocation with an unread selection flag (new output on a
  surface `commands/mem.md` pipes into model context);
- a **new stdout line** on every plain `doctor` run;
- **changed remedy text** in two `doctor` checks;
- **bounded query echo** in both CLI and MCP search labels (a visible output change for long
  queries);
- eleven **removed** `utils.mjs` re-exports (in-tree-only by the commit's argument, but a
  published module surface nonetheless).

Reasoned, not reproduced — if the CHANGELOG stanza and the 5-file version sync are written by
`node cli.mjs release` at tag time, this is a sequencing note rather than a defect. It is
reported because the review is pre-ship and the CLAUDE.md release guard keys on that version
string.

---

## P3-1 — the branch moves the knip reading; `benchmark/hook-latency.mjs` is an unused file, and the CLAUDE.md Baselines row is now wrong

Measured **back-to-back**, same `node_modules`, same Node v26.8.1, same knip, 2026-09-14:

| tree | unused exports | unused files | unlisted binaries |
|---|---|---|---|
| `main` (`git archive main`) | **43** | **0** | 3 |
| `HEAD` (this branch) | **32** | **1** — `benchmark/hook-latency.mjs` | 3 |

`main`'s reading matches the CLAUDE.md Baselines row exactly; HEAD's does not, and the row was
not updated (CLAUDE.md: *"Current reading only — re-measure rather than carry"*).

The new unused file is the new ruler. `knip.json`'s `entry` list registers thirteen
`benchmark/*.mjs` rulers; `hook-latency.mjs` is in none of them and has no npm script, while
`docs/measurement/rulers.md:64` documents it as a ruler. Net effect: a permanent `Unused files
(1)` in the check CLAUDE.md tells the next reader to trust the NAME SET of.

---

## P3-2 — doctor's new "Deeper checks" line is not runnable as printed

`install.mjs:2700` prints, on every plain `doctor` run:

```
  Deeper checks (database layer): claude-mem-lite doctor --benchmark | --metrics | --session-audit
```

Copy-pasted — which is what a line shaped like a command invites — `|` is a shell pipe:

```
$ claude-mem-lite doctor --benchmark | --metrics | --session-audit
bash: line 1: --metrics: command not found
bash: line 1: --session-audit: command not found
```

Same class as 32c8923 ("doctor prescribed a repair binary that was one of the missing files"),
which this branch fixed. Conventional usage-line bracketing (`[--benchmark|--metrics|…]`, no
spaces) removes it. Reproduced.

---

## P3-3 — the notice names the canonical flag, not the flag the user typed

`parseArgs` folds MCP field names onto CLI flags (`cli/common.mjs:36-40`), so
`browse --obs_type bugfix` reports:

```
[mem] --type was ignored — `browse` does not filter on it …
```

The user never typed `--type`. Reproduced.

---

## P3-4 — `search --deep --no-deep` warns that `search` "does not filter on" a flag it does read

`mem-cli.mjs:287-289` resolves the pair with a ternary, so `--deep` short-circuits before
`flags['no-deep']` is touched and the flag is recorded unread. The notice then says `search`
does not filter on `--no-deep` and that the results are UNFILTERED — both false; `--no-deep` is
a real, read `search` flag that lost a precedence contest. Reproduced:

```
$ node cli.mjs search redis --deep --no-deep
[mem] --no-deep was ignored — `search` does not filter on it, so the results above are UNFILTERED. …
```

---

## P3-5 — setup.sh's resolver spawn adds ~27 ms to SessionStart for relocated installs

The new `node -e` resolver block (`scripts/setup.sh:40-58`) is gated on an override being set,
which the comment states as the reason the default path pays nothing. Measured in this tree
(n=5 each, back-to-back, same sandbox HOME, wall-clock ms):

```
control (no override)       : 60 36 36 36 45   → median 36
CLAUDE_MEM_DIR set (spawn)  : 63 75 58 63 68   → median 63
```

≈ **+27 ms median** per SessionStart for users who have relocated. Deliberate and documented;
noted because the branch added `benchmark/hook-latency.mjs` as the ruler for exactly this kind
of cost and this path is not one of its arms.

---

## P3-6 — `install.mjs` now statically imports `lib/doctor-modes.mjs` for a cosmetic hint line

`cli.mjs` goes out of its way to import that module **dynamically** (`cli.mjs:95-103`, with a
docblock explaining why a hard edge in the launcher is unacceptable). `install.mjs:58` imports
it statically, so one more file's absence makes `doctor` — the command for diagnosing a broken
install — unable to run at all. Probed: the failure is caught and explained rather than
crashing, so this is low —

```
$ rm lib/doctor-modes.mjs && node cli.mjs doctor
[claude-mem-lite] This install is incomplete — it is missing: …/lib/doctor-modes.mjs
[claude-mem-lite] Repair: npm install -g claude-mem-lite@latest --force
(real exit code 1)
```

— but the trade is one more file in the diagnostic command's load graph in exchange for a
discoverability line. Reproduced.

---

# What I checked and found CLEAN

**Every new guard goes RED under the mutation it names** (14/14). Each mutation was applied to
a file copy, verified landed (`node --check` / `bash -n` + byte diff), the test run, then the
file restored from the copy and byte-compared against `git show HEAD:<file>`:

| # | mutation | guard | verdict |
|---|---|---|---|
| M1 | add an unregistered `lib/zz-probe.mjs` | lib-registration-complete | RED (2 of 4) |
| M2 | 4th `--newmode` in cli/doctor.mjs, router untouched | doctor-mode-router-sync | RED |
| M3 | rename `reads-` → `readfiles-` in bash only | reads-file-name-cross-language | RED (3 of 3) |
| M4 | revert `readEpisodeRaw` to the inline spelling | episode-buffer-path-single-spelling | RED |
| M5 | `argvParsed = false` | cli-inert-filter-flag | RED |
| M6 | drop the `process.exitCode` gate | cli-inert-filter-flag | RED |
| M7 | setup.sh legacy-DB guard `DB_DIR` → `CODE_DIR` | setup-sh-legacy-db-relocation | RED |
| M8 | `DEPS_FLAG` → `$CODE_DIR/runtime/.deps-broken` | setup-sh-deps-flag-relocation | RED (2 of 6) |
| M9 | drop `&& !deferredOnlyMiss` | get-deferred-miss-once | RED |
| M10 | remove the deeper-modes pointer | doctor-names-deeper-modes | RED |
| M11 | revert `noCodeInstall` scriptRemedy | doctor-remedy-runnable | RED |
| M12 | revert CLI `queryLabel` | query-echo-bounded | RED |
| M13 | re-state the retracted Porter claim in README | fts-tokenizer-claim-sync | RED |
| M14 | drop callLLM's recovery leg | llm-call-error-recovery | RED (3 of 7) |

Tree confirmed byte-identical to `HEAD` afterwards for `mem-cli.mjs`, `install.mjs`,
`cli/common.mjs`, `cli.mjs`, `lib/llm-call.mjs`, `README.md`, `scripts/setup.sh`,
`scripts/post-tool-use.sh`, `cli/doctor.mjs`, `hook-episode.mjs`, `lib/doctor-modes.mjs`,
`format-utils.mjs`.

**The deletions resolve.** A link checker imported all **614** `.mjs`/`.js` files in the tree
under a sandboxed `HOME` and flagged `does not provide an export named` /
`ERR_MODULE_NOT_FOUND`: **0 failures.** The checker was mutation-verified first — my own first
version was a blind instrument (`file://$PWD/...` inside single-quoted `node -e`, so the host
was the literal string `$PWD`; it reported 0 failures without resolving anything). After the
fix, adding `import { TYPE_DECAY_CASE } from './utils.mjs'` produced
`SyntaxError: The requested module './utils.mjs' does not provide an export named
'TYPE_DECAY_CASE'`, and removing it went quiet.
Dynamic `import('…utils.mjs')` sites use only surviving names (`inferProject`,
`sanitizeFtsQuery`, `debugLog`); there are no namespace imports of `utils.mjs`.

**Tarball / manifests.** `npm pack --dry-run` in my tree: 167 files, `lib/doctor-modes.mjs`
present, 0 `benchmark/` files (correct — the two moved modules are imported only by
`benchmark/efficacy-harness.mjs` and their own tests; `tokenize`'s new home
`benchmark/adoption-cosine.mjs` is likewise benchmark-only, and `search-scoring.mjs` still
imports only `porterStem` from `tfidf.mjs`). `npm run validate:manifests` ok for both
manifests. `npx eslint .` exit 0. `npm run format:check` clean.

**Full suite on the ship tree:** `Test Files 2 failed | 403 passed (405)`,
`Tests 2 failed | 6333 passed | 1 skipped (6336)`. Both failures are artifacts of reviewing a
`git archive` extract, not defects:

- `pre-commit-hook-sync > the tracked shim is recorded EXECUTABLE in the index` — runs
  `git ls-files -s`; my tree has no `.git`. The real repo is correct:
  `git -C /home/ai/dev/claude-mem-lite ls-files -s` → `100755 … .githooks/pre-commit`,
  `100755 … scripts/pre-commit.sh`.
- `suite-touches-no-repo-files > adopts the sandbox project … the premise` — the premise needs
  `dogfoodAutoAdopt` to fire, and `install.mjs:1191` gates it on
  `git -C PROJECT_DIR config --get remote.origin.url` matching the sdsrss remote. No `.git`,
  no remote, silent skip. Still fails with `MEM_NO_AUTO_ADOPT` unset, confirming the cause is
  the missing git metadata and not my env.

**Correct-usage sweep for false alarms** (hunt item 1): 145 invocations across three batches,
enumerated by routing path — pre-DB routed commands (`adopt`, `unadopt`, `memdir-audit`),
`doctor`'s two implementations, every DB read command, every write command, every `activity` /
`defer` / `maintain` sub-action, JSON arms, `--help` arms, failing arms, and alias spellings.
Only the four `optimize` invocations in P1-1 (plus the P3-3/P3-4 wording cases) warned.
Notably silent and correct: `memdir-audit --all` (reads `flags.all` via
`_resolveMemdirsForAudit`), `<cmd> --limit N --help` (`runDispatch` returns before `parseArgs`
is ever called, so nothing is recorded as supplied), `restore --project`, `import-jsonl
--project`, `activity search … --limit` (fails first, exit-code gate suppresses).

**The notice is a warning, not a channel change.** `optimize --project demo` exits 0 with the
notice on stderr; `browse --type x` stdout is byte-identical to `browse`. No long-lived process
calls `parseArgs` (only `mem-cli.mjs`, `cli/activity.mjs`, `cli/fts-check.mjs`, all under
`run()`, which calls `resetFlagTracking()` first), so the module-level `suppliedFlags` /
`readFlags` sets cannot accumulate in the MCP server or a hook.

**`setup.sh` re-read against the two fixes.** `shellcheck scripts/setup.sh` clean. No `DATA_DIR`
survivors; every one of the eleven sites now names `CODE_DIR`, `DB_DIR` or `RUNTIME_DIR`, and
each choice matches the `lib/data-paths.mjs` MOVES/STAYS rule the header states (migration and
`node_modules` → `CODE_DIR`; legacy-DB backup and the `claude-mem.db` rename → `DB_DIR`;
`.deps-broken` → `RUNTIME_DIR`; the two run-once markers → `CODE_DIR/runtime`). The resolver
block's failure modes (`set -euo pipefail` + `|| _resolved=""`, both-or-neither assignment,
resolver absent on a truncated tree) all fall back to the pre-existing defaults. The only thing
the rename did not reach is a *different* file — P1-2.
